# Wegmans Shopping App Configuration
# Copy this file to .env and customize as needed

# Database (Required for deployment)
# Get this from Supabase dashboard: Project Settings → Database → Connection string
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres

# Server settings
HOST=0.0.0.0
PORT=8000
DEBUG=false

# Wegmans store settings
STORE_LOCATION=Raleigh
SCRAPER_HEADLESS=true
SEARCH_MAX_RESULTS=10

# Algolia search API (Wegmans product catalog)
# These are public read-only keys - safe to commit defaults, but can override if needed
ALGOLIA_APP_ID=QGPPR19V8V
ALGOLIA_API_KEY=9a10b1401634e9a6e55161c3a60c200d
ALGOLIA_STORE_NUMBER=86  # 86 = Raleigh, NC

# Cache settings
SEARCH_CACHE_DAYS=7
